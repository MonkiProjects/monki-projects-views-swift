import XCTest
@testable import monki_map_ios_views

final class monki_map_ios_viewsTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(monki_map_ios_views().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
